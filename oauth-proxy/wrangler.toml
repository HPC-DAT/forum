name = "hpc-dat-forum-auth"
main = "worker.js"
compatibility_date = "2025-01-01"

[vars]
# Comma-separated list of origins allowed to use this proxy (no paths, no
# trailing slashes)
ALLOWED_ORIGINS = "https://hpc-dat.github.io,http://localhost:5173"
GITHUB_REPOSITORY_ID = "1359601781"
# The client secret is set via
# `wrangler secret put GITHUB_CLIENT_SECRET`
GITHUB_CLIENT_ID = "Iv23li5HMbsnxhnAHYeH"
